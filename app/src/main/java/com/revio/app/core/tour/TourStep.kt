package com.revio.app.core.tour

/** Ordered steps of the first-time guided tour across the main app's bottom navigation. */
enum class TourStep {
    Feed,
    Leaderboard,
    Activity,
    Profile,
    PostCta,
}
